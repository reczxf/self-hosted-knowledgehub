# 个人知识系统技术指导

## 1. 文档目的

本文档用于指导一个“完整个人知识系统”的顶层设计。目标不是实现一个仅能存放资料的知识库，而是构建一个能够持续采集、整理、沉淀、检索、对话和推送的知识操作系统（Personal Knowledge OS）。

本文档重点说明：

- 系统的目标定位
- 为什么它不应只被定义为知识库
- 核心分层设计
- 核心对象模型
- 对话式知识调用方式
- `llm-wiki` 思路如何结合
- 系统设计原则与演进方向

本文档刻意不展开到具体工程实现细节，如数据库表结构、API 设计、任务队列、模型选型等，以便作为后续技术方案设计的上位指导。

---

## 2. 系统定位

### 2.1 本系统不只是知识库

传统意义上的知识库通常具备以下特征：

- 文档被集中存储
- 支持标签与分类
- 提供搜索能力
- 提供基于文档的问答能力

这种系统可以解决“找资料”的问题，但很难完整支持以下需求：

- 连续记录每天的 AI 对话、搜索行为和网页收藏
- 理解“我为什么关注这些内容”
- 将多来源的信息沉淀为长期可复用知识
- 将回答本身继续回写为知识资产
- 基于时间生成日报、周报、复盘与主题推送

因此，本系统更合适的定义是：

> 一个以时间为主轴、以知识页为中间层、以对话为入口的个人知识操作系统。

### 2.2 系统应具备的核心能力

一个完整的个人知识系统应同时具备四类能力：

1. **记录能力**：保存原始资料与行为轨迹
2. **理解能力**：将资料整理为结构化知识
3. **调用能力**：在检索与对话中动态输出相关知识
4. **演化能力**：随着新资料进入不断更新已有认知

这意味着系统不应停留在“文档存储 + RAG”阶段，而应具备持续维护中间知识层的能力。

---

## 3. 设计目标

本系统应支持以下长期目标：

### 3.1 统一接收个人知识来源

来源至少包括：

- AI 对话
- 搜索行为
- 网页收藏
- 文档资料
- 手工笔记
- 后续可扩展到邮件、会议纪要、RSS、代码片段等

### 3.2 保留完整原始事实

系统必须保留原始输入，避免在采集阶段过度改写。原因包括：

- 后续处理逻辑可能变化
- 同一份原始资料可被多次重新整理
- 原始资料是最终可追溯的事实来源

### 3.3 形成持续演化的知识中间层

系统不应每次问答都从原始资料临时拼接答案，而应在新内容进入时持续更新中间知识层，例如：

- 主题页
- 项目页
- 实体页
- 洞察卡片
- 结论页
- 冲突页

### 3.4 支持面向上下文的知识对话

用户提问时，系统不只返回检索到的文档，而应结合：

- 本地原始资料
- 已沉淀的知识页
- 最近关注主题
- 当前会话上下文
- 时间维度上的最新变化

### 3.5 支持时间驱动的推送与复盘

系统需天然支持：

- 日报
- 周报
- 主题订阅
- 复习提醒
- 知识演化跟踪

---

## 4. 总体设计思想

### 4.1 系统的核心不是“检索文档”，而是“维护知识中间层”

仅有原始资料和 RAG 检索的系统，回答能力通常依赖临时召回，存在以下问题：

- 每次问答都重复整理相同材料
- 难以累积长期结论
- 难以表示观点冲突与更新关系
- 难以表达“你过去形成过什么想法”

因此，应在原始资料和问答之间建立一个持久化、可增量维护的知识层。

### 4.2 采用“原始事实层 + 中间知识层 + 对话调用层”结构

推荐的总体认知模型如下：

- 原始层负责记录事实
- 中间层负责沉淀知识
- 对话层负责调用与组织输出
- 时间层负责推送、复盘与演化跟踪

这比单纯的文档向量库更适合长期个人知识管理。

---

## 5. 五层系统架构

建议将整个系统抽象为五层。

### 5.1 Source Layer：原始事实层

这一层保存所有原始输入，是系统的事实基础。

包含内容：

- AI 对话原文
- 搜索事件
- 网页原文
- PDF / 文档原件
- 手工笔记
- 外部导入材料

这一层的设计原则：

- 尽量完整保存
- 保留来源与时间
- 尽量不可变
- 可追溯到原始出处

这一层解决的问题不是“如何回答”，而是“事实从哪里来”。

### 5.2 Memory Layer：记忆与索引层

这一层用于提高召回能力，不直接承担最终知识表达。

典型内容包括：

- 全文索引
- 向量索引
- 标签索引
- 时间索引
- 实体索引
- 项目索引

这一层本质上是系统的“记忆入口”，主要服务于高效检索和候选召回。

### 5.3 Knowledge Layer：知识编译层

这是系统最核心的一层。

该层将原始资料整理为可持续更新的知识对象，例如：

- 主题页
- 项目页
- 人物 / 实体页
- 时间线页
- 问题页
- 洞察卡片
- 冲突页
- 待验证页

这一层不应是一次性生成结果，而应是随着新输入持续更新的“知识编译产物”。

### 5.4 Conversation Layer：对话执行层

这一层是用户直接交互的入口。

系统在对话中回答问题时，应优先结合：

1. 已沉淀知识页
2. 相关索引与召回结果
3. 原始资料作为证据引用

也就是说：

- 知识页负责表达理解
- 原始文档负责提供证据
- 会话上下文负责组织输出

### 5.5 Time Layer：时间与推送层

这一层使系统具备“活性”。

典型能力包括：

- 每日新增汇总
- 每周主题回顾
- 某主题最近变化
- 某结论被更新或推翻
- 旧知识复习提醒
- 新旧认知对比

这一层让系统从“被动查询”转为“主动服务”。

---

## 6. 核心对象模型

建议从概念上定义四类核心对象。

### 6.1 Event：行为事件

Event 表示用户的认知行为轨迹，例如：

- 某次搜索
- 某次 AI 提问
- 某次网页收藏
- 某次知识整理
- 某次问答结果回写

Event 的核心价值在于记录“什么时候发生了什么行为”。

它不一定本身是知识，但能帮助系统理解：

- 当前关注主题
- 关注变化趋势
- 某个知识对象为何出现
- 什么内容值得被主动推送

### 6.2 Source：原始材料

Source 表示进入系统的原始对象，例如：

- 对话记录
- 网页
- PDF
- 笔记
- 搜索结果

Source 强调其原始性与可追溯性，是知识整理的事实来源。

### 6.3 Knowledge：知识对象

Knowledge 表示从原始资料中沉淀出来的中间层知识。

知识对象可以表现为：

- 某主题的总结
- 某项目的状态页
- 某问题的已知答案
- 某洞察的结构化表达
- 某冲突观点的并列表达

Knowledge 是对话系统最应优先使用的材料。

### 6.4 Session：交互上下文

Session 表示用户当前会话上下文，包括：

- 当前问题
- 当前项目
- 当前主题偏好
- 最近关注方向
- 最近使用的相关知识页

Session 的作用是让系统的回答不只是“查到什么”，而是“在当前上下文下最应该输出什么”。

---

## 7. 为什么必须引入知识中间层

### 7.1 传统 RAG 的局限

如果系统只依赖“原始文档 + 检索 + LLM 生成”，会出现以下问题：

- 相同问题每次都要重新整理
- 很难形成稳定结论
- 缺少长期主题汇总
- 无法自然表示知识的更新和冲突
- 回答无法体现历史思考过程

### 7.2 知识中间层的价值

知识中间层使系统可以：

- 将长期主题整理为持续更新的页面
- 将一次高质量回答沉淀为长期知识
- 显式维护已知结论与待验证问题
- 将多个来源整合为统一表述
- 降低重复推理成本

### 7.3 中间层应具备的特征

一个合格的知识中间层应具备：

- 持久化
- 可增量更新
- 可读
- 可被再次引用
- 可被修订
- 可链接到原始证据

---

## 8. 对话式知识系统的回答方式

### 8.1 不应只返回相关文档

如果用户提问时，系统只返回“相关文档列表”，那它仍然只是一个检索系统。

更理想的回答结构应包括三层：

#### 第一层：直接回答

先给出对当前问题的归纳结论。

#### 第二层：相关知识对象

输出系统中已经沉淀的：

- 主题页
- 项目页
- 洞察卡片
- 历史结论

#### 第三层：支撑原始资料

列出支撑上述回答的原始对话、网页、文档、搜索记录等。

### 8.2 这种结构的优势

这种回答结构同时满足：

- 快速理解
- 证据追溯
- 知识复用
- 长期沉淀

它比单纯“给文档片段 + 模型生成”的方式更接近真正的知识助手。

---

## 9. llm-wiki 可借鉴的核心思想

`llm-wiki` 非常适合作为本系统知识编译层的理念参考。

其最值得吸收的不是某个具体实现，而是以下原则。

### 9.1 原始资料与中间知识层分离

原始资料是 source of truth，中间 wiki 是 LLM 维护的持久产物。

这意味着：

- 原始内容尽量不可变
- 知识页允许持续修订
- 回答时优先使用知识页，但必须能回溯到原始来源

### 9.2 在 ingest 阶段做整合，而不只在 query 阶段检索

新资料进入系统时，不能只是被动入库。

更好的方式是：

- 识别它与哪些主题相关
- 更新哪些知识页
- 是否冲击已有结论
- 是否需要新增洞察卡片或问题页

这样系统会随着输入不断成长，而不是永远依赖临时问答。

### 9.3 高质量回答本身应回写知识层

用户与系统的一次高质量对话，本身也是高价值知识对象。

因此，回答后的产物不应消失在聊天记录里，而应在条件满足时回写为：

- 主题总结页
- 洞察卡片
- 问题页
- 项目页更新

### 9.4 需要周期性的知识健康检查

知识系统不是一次构建后永久正确，它需要持续维护。

可定期检查：

- 过时页面
- 冲突结论
- 孤立页面
- 缺少交叉引用的主题
- 原始资料已大量增加但知识页未更新的主题

---

## 10. llm-wiki 思路在本系统中的扩展

虽然 `llm-wiki` 的思路很适合，但本系统还需要在其基础上扩展三类能力。

### 10.1 扩展事件层

本系统不仅处理文档，还处理行为事件。

例如：

- 今天搜索了什么
- 今天问了 AI 什么
- 今天收藏了什么

这些事件决定了：

- 当前关注主题
- 推荐与推送逻辑
- 复盘时的时间线结构

因此，本系统必须显式管理 Event，而不只是管理 markdown 页面。

### 10.2 扩展时间层

时间不是附属属性，而是主轴之一。

系统需要同时关注：

- 原始内容发布时间
- 用户采集时间
- 问题发生时间
- 知识更新时间
- 推荐触发时间
- 复习时间

时间层使系统具备“追踪演化”而不只是“静态存储”的能力。

### 10.3 扩展混合检索能力

由于输入类型复杂，系统不能只依赖 markdown 页面导航。

需要支持：

- 知识页检索
- 原始资料全文检索
- 向量召回
- 标签过滤
- 时间过滤
- 实体关联

也就是说，`llm-wiki` 更适合作为知识组织方法，而不是完整的检索解决方案。

---

## 11. 推荐的知识表现形式

建议采用“三种视图并存”的方式。

### 11.1 Wiki 视图

适合人类阅读与浏览。

适合表达：

- 主题总结
- 项目概览
- 问题与答案
- 时间线概览

### 11.2 Graph 视图

适合机器理解关系。

适合表达：

- 实体关系
- 主题关联
- 项目与资料之间的依赖
- 观点冲突与引用链路

### 11.3 Card 视图

适合推送、对话与快速消费。

适合表达：

- 一条洞察
- 一条结论
- 一个待验证问题
- 一段高价值回答

因此，不建议在 wiki、图谱、卡片之间三选一，而应将三者统一为同一知识系统的不同观察方式。

---

## 12. 系统设计原则

### 12.1 原始资料优先保留

所有高层知识都应能回溯到原始输入。

### 12.2 知识层可持续重写

知识页不是一次性产物，应允许：

- 合并
- 修订
- 更新
- 增量补充
- 标记冲突与过时

### 12.3 回答结果应尽量沉淀

高质量回答不应只存在于聊天记录中。

### 12.4 时间是一级维度

时间不应只是 metadata，而应直接参与：

- 查询
- 排序
- 推送
- 复盘
- 知识健康检查

### 12.5 检索与知识维护解耦

检索负责召回，知识层负责表达，二者不应混为一谈。

### 12.6 允许人工干预

系统生成的知识对象不应完全不可编辑。

理想状态下，人可以：

- 修订标签
- 修正主题页
- 合并知识卡片
- 标记重要性
- 调整推送策略

---

## 13. 系统的终极产品形态

如果系统演进成熟，其最终应表现为：

### 13.1 一个长期记忆体

它记得你看过什么、想过什么、问过什么、总结过什么。

### 13.2 一个知识编译器

它把原始资料持续编译成可消费、可引用、可更新的知识层。

### 13.3 一个对话式知识助手

它在你提问时，不只是“找文档”，而是“调用你的知识系统”。

### 13.4 一个时间驱动的信息操作台

它会主动告诉你：

- 最近什么有价值
- 哪些主题在增长
- 哪些想法发生了变化
- 哪些旧知识值得回顾

---

## 14. 结论

本系统最终不应只做成“知识库”，而应构建为一个完整的个人知识操作系统。

其关键不在于把资料全部收集起来，而在于在原始资料与对话之间建立一个持续维护的知识中间层。这个中间层应以 wiki / 卡片 / 图谱等形式存在，并随新资料进入而持续演化。

`llm-wiki` 提供了非常有价值的设计方向，尤其适合作为本系统知识编译层的指导思想：

- 原始资料不可随意改写
- 中间知识层应持久化
- 在 ingest 阶段而不只在 query 阶段做整合
- 问答结果应回写
- 系统应具备周期性的知识健康检查能力

在此基础上，再加入事件流、时间维度和混合检索能力，就可以形成一个真正面向长期使用的个人知识系统。

---

## 15. 后续建议

在本文档基础上，后续可以继续细化三份下位文档：

1. **系统对象模型文档**
   - Event / Source / Knowledge / Session 的精确定义与关系

2. **系统分层与数据流文档**
   - 从采集到知识编译到对话输出的完整路径

3. **技术实现方案文档**
   - 数据存储、索引、任务流、模型调用、检索与推送实现

这三份文档可以从产品指导、架构设计进一步落到工程执行。